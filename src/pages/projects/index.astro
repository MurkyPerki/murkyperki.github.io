---
import Layout from "../../layouts/Layout.astro";
import projects from "../../projects.data.js";
---
<Layout title="Projects — Max Burkhardt">
  <h2>Projects</h2>
  <div class="grid">
    {projects.map(p => (
      <a class="card" href={`/projects/${p.slug}/`}>
        <h3 style="margin:.2rem 0;">{p.title}</h3>
        <p class="muted" style="margin:.2rem 0;">{p.pitch}</p>
        <small class="muted">{p.role}</small>
      </a>
    ))}
  </div>
</Layout>
